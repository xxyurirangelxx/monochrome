# Configuração Docker do Monochrome
# Copie para .env e edite: cp .env.example .env

# --- Monochrome (Frontend) ---
MONOCHROME_PORT=8080
MONOCHROME_DEV_PORT=8080

# --- Portão de Autenticação (autenticação server-side) ---
# Defina AUTH_ENABLED=true para ativar o portão de autenticação (login obrigatório)
AUTH_ENABLED=false
AUTH_SECRET=mude-para-uma-string-aleatoria
FIREBASE_PROJECT_ID=monochrome-database
# Opcional: habilitar/desabilitar provedores de login (padrão: true quando não definido)
# AUTH_GOOGLE_ENABLED=true
# AUTH_EMAIL_ENABLED=true
# Opcional: sobrescrever a configuração do Firebase para a página de login (string JSON)
# FIREBASE_CONFIG={"apiKey":"...","authDomain":"...","projectId":"...","storageBucket":"...","messagingSenderId":"...","appId":"..."}
# URL do PocketBase apontando para a instância local self-hosted
POCKETBASE_URL=http://localhost:7284
# SESSION_MAX_AGE=604800000  # 7 dias em ms (padrão)

# --- PocketBase (banco de dados local self-hosted) ---
POCKETBASE_PORT=7284
PB_ADMIN_EMAIL=admin@example.com
PB_ADMIN_PASSWORD=changeme
TZ=America/Sao_Paulo
